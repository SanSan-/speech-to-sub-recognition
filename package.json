{
  "name": "speech-to-sub-recognition-e2e",
  "private": true,
  "scripts": {
    "test": "playwright test --config=playwright.config.mjs"
  },
  "devDependencies": {
    "@playwright/test": "1.58.2"
  }
}
